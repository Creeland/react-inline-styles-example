const baseRadius = '.5rem';
const primaryColor = '#60B9D7';
const styles = {
  announcement: {
    borderRadius: baseRadius,
    border: `4px solid ${primaryColor}`,
    padding: '.5rem',
    fontFamily: 'sans-serif',
    fontSize: '1.3rem',
    textAlign: 'center',
    background: `${primaryColor}`,
    color: `#fff`
  }
};

export default styles;
